name: quality-android

on:
  pull_request:
    branches:
      - main
    paths:
      - "androidApp/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"
      - ".github/workflows/quality-module-reusable.yml"
      - ".github/scripts/upsert-quality-comment.cjs"
      - ".github/workflows/quality-android.yml"
  push:
    branches:
      - main
      - "codex/**"
    paths:
      - "androidApp/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"
      - ".github/workflows/quality-module-reusable.yml"
      - ".github/scripts/upsert-quality-comment.cjs"
      - ".github/workflows/quality-android.yml"
  workflow_dispatch:

concurrency:
  group: quality-android-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  quality:
    uses: ./.github/workflows/quality-module-reusable.yml
    with:
      module_key: android
      module_title: Android Module
      runner: ubuntu-latest
      lint_cmd: "spotlessCheck"
      lint_label: "spotlessCheck"
      detekt_cmd: ":androidApp:detekt"
      detekt_label: ":androidApp:detekt"
      test_cmd: ":androidApp:testDebugUnitTest"
      test_label: ":androidApp:testDebugUnitTest"
      assemble_cmd: ":androidApp:assembleDebug"
      assemble_label: ":androidApp:assembleDebug"
      kover_cmd: ":androidApp:koverHtmlReport"
      kover_label: ":androidApp:koverHtmlReport"
      kover_html_report_dir: androidApp/build/reports/kover/html
      coverage_module: androidApp
      coverage_title: Android Line Coverage
    secrets: inherit
