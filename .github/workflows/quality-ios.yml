name: quality-ios

on:
  pull_request:
    branches:
      - main
    paths:
      - "iosApp/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"
      - ".github/workflows/quality-ios.yml"
  push:
    branches:
      - main
      - "codex/**"
    paths:
      - "iosApp/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"
      - ".github/workflows/quality-ios.yml"
  workflow_dispatch:

concurrency:
  group: quality-ios-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CI: true
  GRADLE_OPTS: -Dorg.gradle.daemon=false -Dorg.gradle.jvmargs=-Xmx4g

jobs:
  module-quality:
    name: Quality Gate - iOS Module
    runs-on: macos-15
    timeout-minutes: 45
    outputs:
      build: ${{ steps.collect.outputs.build }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build iOS simulator app
        id: build
        continue-on-error: true
        run: ./gradlew :iosApp:buildIosSimulatorApp

      - name: Collect outputs
        id: collect
        if: always()
        env:
          BUILD_RESULT: ${{ steps.build.outcome }}
        run: |
          echo "build=${BUILD_RESULT}" >> "$GITHUB_OUTPUT"

      - name: Publish summary
        if: always()
        env:
          BUILD_RESULT: ${{ steps.collect.outputs.build }}
        run: |
          status_icon() {
            case "$1" in
              success) echo "✅ success" ;;
              failure) echo "❌ failure" ;;
              cancelled) echo "⚪ cancelled" ;;
              skipped) echo "⚪ skipped" ;;
              *) echo "⚪ unknown" ;;
            esac
          }

          echo "### iOS Module Quality Gate" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Check | Result |" >> "$GITHUB_STEP_SUMMARY"
          echo "|---|---|" >> "$GITHUB_STEP_SUMMARY"
          echo "| \`:iosApp:buildIosSimulatorApp\` | $(status_icon "$BUILD_RESULT") |" >> "$GITHUB_STEP_SUMMARY"

      - name: Enforce gate
        if: always()
        env:
          BUILD_RESULT: ${{ steps.collect.outputs.build }}
        run: |
          if [ "$BUILD_RESULT" != "success" ]; then
            echo "iOS quality gate failed."
            exit 1
          fi

      - name: Upload reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ios-quality-reports
          path: |
            **/build/reports/**
            **/build/test-results/**
          if-no-files-found: ignore

  report-pr:
    name: Quality Gate - iOS PR Comment
    runs-on: ubuntu-latest
    needs:
      - module-quality
    if: ${{ github.event_name == 'pull_request' && always() }}
    concurrency:
      group: quality-gate-pr-comment-${{ github.event.pull_request.number }}
      cancel-in-progress: false
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Post iOS quality comment
        uses: actions/github-script@v7
        env:
          WORKFLOW_RESULT: ${{ needs.module-quality.result }}
          BUILD_RESULT: ${{ needs.module-quality.outputs.build }}
        with:
          script: |
            const marker = '<!-- quality-gate-report -->'
            const currentShaMarker = `<!-- quality-gate-sha:${context.payload.pull_request.head.sha} -->`
            const sectionStart = '<!-- quality-gate-section:ios:start -->'
            const sectionEnd = '<!-- quality-gate-section:ios:end -->'
            const legacyMarkers = [
              '<!-- quality-gate-android -->',
              '<!-- quality-gate-desktop -->',
              '<!-- quality-gate-ios -->',
              '<!-- quality-gate-core-common -->',
              '<!-- quality-gate-core-database -->',
              '<!-- quality-gate-core-ui -->',
              '<!-- quality-gate-feature-notes -->'
            ]
            const toBadge = (value) => {
              switch (value) {
                case 'success':
                  return '✅ success'
                case 'failure':
                  return '❌ failure'
                case 'cancelled':
                  return '⚪ cancelled'
                case 'skipped':
                  return '⚪ skipped'
                default:
                  return '⚪ unknown'
              }
            }

            const section = `${sectionStart}
            ### iOS Module

            | Check | Result |
            |---|---|
            | Workflow overall | ${toBadge(process.env.WORKFLOW_RESULT)} |
            | \`:iosApp:buildIosSimulatorApp\` | ${toBadge(process.env.BUILD_RESULT)} |
            ${sectionEnd}`.trim()

            const { owner, repo } = context.repo
            const issue_number = context.payload.pull_request.number
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number,
              per_page: 100
            })
            let existing = comments.find((comment) =>
              comment.user?.type === 'Bot' && comment.body?.includes(marker)
            )

            if (!existing) {
              existing = comments.find((comment) =>
                comment.user?.type === 'Bot' && legacyMarkers.some((legacyMarker) =>
                  comment.body?.includes(legacyMarker)
                )
              )
            }

            const escapeForRegex = (value) => value.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')
            const shaRegex = /<!-- quality-gate-sha:[0-9a-f]+ -->/
            const sectionRegex = new RegExp(
              `${escapeForRegex(sectionStart)}[\\s\\S]*?${escapeForRegex(sectionEnd)}`
            )

            let body
            if (!existing || !existing.body?.includes(currentShaMarker)) {
              body = `${marker}
            ${currentShaMarker}
            ### Quality Gate Report`
            } else {
              body = existing.body
              body = body.replace(shaRegex, currentShaMarker)
            }

            if (sectionRegex.test(body)) {
              body = body.replace(sectionRegex, section)
            } else {
              body = `${body.trim()}

            ${section}`
            }

            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body
              })
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body
              })
            }
