name: quality-core-database

on:
  pull_request:
    branches:
      - main
    paths:
      - "core/database/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"
      - ".github/workflows/quality-module-reusable.yml"
      - ".github/scripts/upsert-quality-comment.cjs"
      - ".github/workflows/quality-core-database.yml"
  push:
    branches:
      - main
      - "codex/**"
    paths:
      - "core/database/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"
      - ".github/workflows/quality-module-reusable.yml"
      - ".github/scripts/upsert-quality-comment.cjs"
      - ".github/workflows/quality-core-database.yml"
  workflow_dispatch:

concurrency:
  group: quality-core-database-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  quality:
    uses: ./.github/workflows/quality-module-reusable.yml
    with:
      module_key: core-database
      module_title: core:database Module
      runner: ubuntu-latest
      lint_cmd: ":core:database:lintAnalyzeAndroidHostTest"
      lint_label: ":core:database:lintAnalyzeAndroidHostTest"
      test_cmd: ":core:database:allTests"
      test_label: ":core:database:allTests"
      assemble_cmd: ":core:database:assemble"
      assemble_label: ":core:database:assemble"
      kover_cmd: ":core:database:koverXmlReport"
      kover_label: ":core:database:koverXmlReport"
      kover_report_path: core/database/build/reports/kover/report.xml
      coverage_module: core:database
      coverage_title: core:database Line Coverage
    secrets: inherit
