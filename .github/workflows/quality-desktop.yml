name: quality-desktop

on:
  pull_request:
    branches:
      - main
    paths:
      - "desktopApp/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"
      - ".github/workflows/quality-module-reusable.yml"
      - ".github/scripts/upsert-quality-comment.cjs"
      - ".github/workflows/quality-desktop.yml"
  push:
    branches:
      - main
      - "codex/**"
    paths:
      - "desktopApp/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"
      - ".github/workflows/quality-module-reusable.yml"
      - ".github/scripts/upsert-quality-comment.cjs"
      - ".github/workflows/quality-desktop.yml"
  workflow_dispatch:

concurrency:
  group: quality-desktop-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  quality:
    uses: ./.github/workflows/quality-module-reusable.yml
    with:
      module_key: desktop
      module_title: Desktop Module
      runner: ubuntu-latest
      test_cmd: ":desktopApp:test"
      test_label: ":desktopApp:test"
      assemble_cmd: ":desktopApp:assemble"
      assemble_label: ":desktopApp:assemble"
      kover_cmd: ":desktopApp:koverXmlReport"
      kover_label: ":desktopApp:koverXmlReport"
      kover_report_path: desktopApp/build/reports/kover/report.xml
      coverage_module: desktopApp
      coverage_title: Desktop Line Coverage
    secrets: inherit
